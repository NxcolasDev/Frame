services:

  postgres:

    image: postgres:16

    container_name: frame-db

    restart: always

    environment:

      POSTGRES_USER: frame

      POSTGRES_PASSWORD: frame123

      POSTGRES_DB: frame

    ports:

      - "5432:5432"

    volumes:

      - postgres_data:/var/lib/postgresql/data

volumes:

  postgres_data: